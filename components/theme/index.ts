import type { ThemeConfig } from 'ant-design-vue/es/config-provider/context';
export { theme } from 'ant-design-vue';

// 自定义默认主题配置
export const defaultTheme: ThemeConfig = {
  token: {
    // 自定义主题色
    // colorPrimary: '#e75213',
    // 可以添加其他自定义tokencolorPrimary
  },
};
